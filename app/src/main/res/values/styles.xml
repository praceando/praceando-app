<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="CalenderViewCustom" parent="Theme.AppCompat">
        <item name="colorControlActivated">@color/rosaEscuro</item>
        <item name="android:textColorPrimary">@color/rosaEscuro</item>
    </style>

    <style name="CalenderViewWeekCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/rosaClaro</item>
    </style>

    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.Holo.Small">
        <item name="android:textColor">@color/date_color_selector</item>
        <item name="android:weekNumberColor">@color/rosaEscuro</item>
    </style>

    <style name="CustomRatingBarStyle" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_custom</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dip</item>
    </style>

    <style name="CustomRatingBarStyleSmall" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_custom_small</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dip</item>
    </style>

    <!-- Tema personalizado para o DatePicker -->
    <style name="CustomDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/rosa</item>
    </style>

    <!-- Tema personalizado para o TimePicker -->
    <style name="CustomTimePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/rosa</item>
    </style>



</resources>
